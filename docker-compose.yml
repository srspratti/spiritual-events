services:
  postgres:
    image: postgres:16-alpine
    container_name: spiritual-events-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: spiritual_events
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d spiritual_events"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
